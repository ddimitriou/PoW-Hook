name: Proof of Work Validator

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - main

jobs:
  verify-pow:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python environment
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: pip install cryptography

      - name: Verify Proof-of-Work Tokens
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          POW_ADMIN_HANDLES: ${{ secrets.POW_ADMIN_HANDLES }}
          POW_GITHUB_API_URL: ${{ secrets.POW_GITHUB_API_URL }}
          POW_CHECKS_CMD: 'none' # The expected custom checks command value or "none"
        run: python3 .github/scripts/verify_pow.py
