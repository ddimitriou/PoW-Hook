# Optional: Define the command or script to execute for your custom validation checks
# POW_CHECKS_CMD="./custom-checks.py"

# Optional: List GitHub Handles separated by spaces to notify in PR comments (e.g., "@sfitsos @security")
# POW_ADMIN_HANDLES="@sfitsos"

# Required for server-side attestation (GitHub Actions mode only):
# A GitHub Personal Access Token with 'actions' scope to trigger workflow_dispatch events.
# GITHUB_PAT_LOCAL="ghp_..."

# Required for server-side attestation (GitHub Actions mode only):
# The owner/repo slug of this repository (e.g., "sfitsos/pow-hook").
# POW_GITHUB_REPO="owner/repo"
